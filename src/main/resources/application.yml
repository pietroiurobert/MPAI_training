spring:
    application:
      name: exam-2025

    datasource:
        url: jdbc:h2:mem:mpaidb
        driver-class-name: org.h2.Driver
        username: sa
        password:
    jpa:
        hibernate:
            ddl-auto: create
    h2:
        console:
            enabled: true
            path: /h2-console